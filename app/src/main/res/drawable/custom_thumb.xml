<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#FFD700" /> <!-- Fallback to Gold if accent_gold missing, but wil check -->
    <size
        android:width="16dp"
        android:height="16dp" />
    <stroke
        android:width="2dp"
        android:color="#80000000" />
</shape>
